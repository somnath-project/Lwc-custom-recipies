import { LightningElement } from 'lwc';

export default class ParentComponent extends LightningElement {

    greeting="Welcome to Ankit jain LWC";

    userDetails={
        firstName: "Ankit",
        LastName: "Ekhande"
    };
}